@{
    Layout = "_CmsLayout";
    ViewData["Title"] = "Access denied";
}
<section class="cms-empty">
    <span class="cms-eyebrow">Authorization required</span>
    <h1>Access denied</h1>
    <p>Your account does not have permission to open this area.</p>
    <a class="cms-button cms-button-primary" asp-controller="Admin" asp-action="Dashboard">Return to dashboard</a>
</section>
